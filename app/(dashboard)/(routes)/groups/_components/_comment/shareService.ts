// // import { IComment } from "@/interfaces/comment-interface";
// import { BehaviorSubject } from "rxjs";

// // Create a BehaviorSubject with an initial value of null
// const observableModel = new BehaviorSubject<any | null>(null);
// // const observableModel = new BehaviorSubject<IComment | null>(null);

// // Export the observable model
// export default observableModel;
// export const isShowReplyForm = new BehaviorSubject<boolean>(true);
